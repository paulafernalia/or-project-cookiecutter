[project]
name = "mip-cookieutter"
version = "0.1.0"
description = "Cookiecutter template for mip projects"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cookiecutter>=2.6.0",
]

[dependency-groups]
dev = [
    "ruff>=0.8.0",
    "mypy>=1.17.0",
    "prek>=0.3.10",
]
